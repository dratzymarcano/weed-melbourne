# Sitemap — serve sitemap-index.xml content at /sitemap.xml
/sitemap.xml       /sitemap-index.xml       200

# SPA client-side routes (rewrite, not redirect)
/account/    /account/    200
/cart/       /cart/       200
/checkout/   /checkout/   200
