---
const faqs = [
  {
    question: "How do I buy weed online in Australia?",
    answer: "Simple: browse our product range, select your items, and add them to your cart. Complete checkout with secure payment, and your order ships discreetly to your address. Most orders arrive within 2-5 business days."
  },
  {
    question: "How much does weed cost?",
    answer: "Prices vary by product — flower from $180-300/oz, THC gummies from $280-660 per 100-pack, vapes from $250-485 per 5-pack. All prices are on the product pages. No hidden fees, no 'contact for pricing' games."
  },
  {
    question: "Is my order shipped discreetly?",
    answer: "100%. Plain packaging, no logos, no indication of contents. Discreet billing descriptions too. We've shipped thousands of orders — your privacy is our priority."
  },
  {
    question: "Are your products lab-tested?",
    answer: "Every single one. We test for THC/CBD content, pesticides, heavy metals, and contaminants. Lab reports available on each product page. We don't sell mystery product."
  },
  {
    question: "Do you deliver weed near me?",
    answer: "If you're in Australia, yes. Sydney, Melbourne, Brisbane, Perth, Adelaide, regional areas — anywhere Australia Post delivers. Express shipping available for most locations."
  },
  {
    question: "What payment methods do you accept?",
    answer: "Credit cards, debit cards, and bank transfers. All processed securely with discreet billing. Your bank statement won't say 'weed shop' on it."
  }
];
---

<section class="section faq-section">
  <div class="container">
    <div class="content-grid">
      <div class="content-text">
        <span class="section-label">COMMON QUESTIONS</span>
        <h2>How to Buy Weed Online — Your Questions Answered</h2>
        
        <div class="faq-list">
          {faqs.map((faq, index) => (
            <div class="faq-item" data-index={index}>
              <button class="faq-question" aria-expanded="false">
                <span>{faq.question}</span>
                <svg class="faq-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <polyline points="6 9 12 15 18 9"></polyline>
                </svg>
              </button>
              <div class="faq-answer">
                <p>{faq.answer}</p>
              </div>
            </div>
          ))}
        </div>

        <a href="/faq" class="btn btn-outline-dark">View All FAQs</a>
      </div>
      <div class="content-image">
        <div class="image-placeholder">
          <span>📦</span>
          <p>Discreet & Fast Delivery</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .faq-section {
    background: var(--color-white);
  }

  .content-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: start;
  }

  .content-text h2 {
    color: var(--color-primary);
    margin-bottom: 30px;
  }

  .faq-list {
    margin-bottom: 30px;
  }

  .faq-item {
    border: 1px solid rgba(0, 0, 0, 0.1);
    border-radius: 8px;
    margin-bottom: 15px;
    overflow: hidden;
  }

  .faq-question {
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 18px 20px;
    background: var(--color-bg);
    border: none;
    cursor: pointer;
    text-align: left;
    font-family: var(--font-family);
    font-size: 1rem;
    font-weight: 500;
    color: var(--color-text);
    transition: all 0.3s ease;
  }

  .faq-question:hover {
    background: var(--color-primary);
    color: var(--color-white);
  }

  .faq-icon {
    transition: transform 0.3s ease;
    flex-shrink: 0;
    margin-left: 15px;
  }

  .faq-item.active .faq-icon {
    transform: rotate(180deg);
  }

  .faq-item.active .faq-question {
    background: var(--color-primary);
    color: var(--color-white);
  }

  .faq-answer {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease;
  }

  .faq-item.active .faq-answer {
    max-height: 500px;
  }

  .faq-answer p {
    padding: 20px;
    color: var(--color-text);
    line-height: 1.8;
    margin: 0;
  }

  .btn-outline-dark {
    display: inline-block;
    padding: 12px 30px;
    border: 2px solid var(--color-primary);
    color: var(--color-primary);
    font-weight: 600;
    border-radius: 4px;
    transition: all 0.3s ease;
  }

  .btn-outline-dark:hover {
    background: var(--color-primary);
    color: var(--color-white);
  }

  .image-placeholder {
    background: linear-gradient(135deg, #9b59b6 0%, #8e44ad 100%);
    border-radius: 12px;
    padding: 60px;
    text-align: center;
    color: var(--color-white);
    min-height: 400px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    position: sticky;
    top: 120px;
  }

  .image-placeholder span {
    font-size: 6rem;
    margin-bottom: 20px;
  }

  .image-placeholder p {
    font-size: 1.1rem;
    font-weight: 600;
  }

  @media (max-width: 992px) {
    .content-grid {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .content-image {
      order: -1;
    }

    .image-placeholder {
      min-height: 250px;
      padding: 40px;
      position: static;
    }

    .image-placeholder span {
      font-size: 4rem;
    }
  }
</style>

<script>
  const faqItems = document.querySelectorAll('.faq-item');
  
  faqItems.forEach(item => {
    const question = item.querySelector('.faq-question');
    
    question?.addEventListener('click', () => {
      const isActive = item.classList.contains('active');
      
      // Close all items
      faqItems.forEach(i => i.classList.remove('active'));
      
      // Open clicked item if it wasn't active
      if (!isActive) {
        item.classList.add('active');
      }
    });
  });
</script>
