---

---

<section class="section why-buying">
  <div class="container">
    <div class="content-grid">
      <div class="content-text">
        <span class="section-label">WHY BUY WEED ONLINE</span>
        <h2>Skip the Hassle — Here's Why People Choose Us</h2>
        <p>
          Look, we get it. Buying weed in Australia has always been a pain. Unreliable sources, sketchy meetups, or overpriced "premium" product that turns out to be average. We've been there, and that's exactly why Mullaways exists.
        </p>
        <p>
          <strong>What makes us different?</strong>
        </p>
        <ul class="benefit-list">
          <li><strong>No More Waiting Around:</strong> Order online, get it delivered. Done. No more waiting for texts back or hoping someone shows up.</li>
          <li><strong>Actually Discreet:</strong> Plain packaging, no logos, discreet billing. Your business is your business.</li>
          <li><strong>You Know What You're Getting:</strong> Lab-tested for potency and purity. The THC percentage on the label? That's actually what's in there.</li>
          <li><strong>Real People, Real Support:</strong> Got questions? We answer them. Problem with an order? We fix it. No runaround.</li>
          <li><strong>Fair Prices:</strong> How much does weed cost? Check our prices — we're upfront about everything. No "contact for pricing" nonsense.</li>
          <li><strong>Delivery That Arrives:</strong> Australia-wide shipping with tracking. Most orders land in 2-5 days, often sooner.</li>
        </ul>
        <p>
          Thousands of Aussies have made the switch. <a href="/shop">See what we've got →</a>
        </p>
      </div>
      <div class="content-image">
        <div class="image-placeholder">
          <span>📦</span>
          <p>Discreet Delivery Australia-Wide</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .why-buying {
    background: var(--color-white);
  }

  .content-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
  }

  .content-text h2 {
    color: var(--color-primary);
    margin-bottom: 25px;
  }

  .content-text p {
    color: var(--color-text);
    line-height: 1.8;
    margin-bottom: 20px;
  }

  .content-text a {
    color: var(--color-accent);
    font-weight: 600;
  }

  .content-text a:hover {
    color: var(--color-primary);
  }

  .benefit-list {
    margin: 20px 0 30px 20px;
  }

  .benefit-list li {
    position: relative;
    padding-left: 25px;
    margin-bottom: 15px;
    color: var(--color-text);
    line-height: 1.7;
  }

  .benefit-list li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: bold;
  }

  .image-placeholder {
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
    border-radius: 12px;
    padding: 60px;
    text-align: center;
    color: var(--color-white);
    min-height: 400px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  .image-placeholder span {
    font-size: 6rem;
    margin-bottom: 20px;
  }

  .image-placeholder p {
    font-size: 1.2rem;
    font-weight: 600;
  }

  @media (max-width: 992px) {
    .content-grid {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .content-image {
      order: -1;
    }

    .image-placeholder {
      min-height: 300px;
      padding: 40px;
    }

    .image-placeholder span {
      font-size: 4rem;
    }
  }
</style>
