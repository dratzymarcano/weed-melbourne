---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Breadcrumb from '../components/Breadcrumb.astro';

const title = "Pay with Bank Transfer | Buy Cannabis Australia";
const description = "Pay for cannabis in Australia using bank transfer. Send via PayID for instant payment or use BSB/account transfer. No extra fees, simple and reliable.";

const schema = {
  "@context": "https://schema.org",
  "@type": "HowTo",
  "name": "How to Pay with Bank Transfer in Australia",
  "description": "A step-by-step guide to paying for cannabis online using Australian bank transfer, PayID, or Osko.",
  "step": [
    {
      "@type": "HowToStep",
      "name": "Place Your Order",
      "text": "Browse our shop, add products to your cart, fill in your delivery details, and select Bank Transfer as your payment method."
    },
    {
      "@type": "HowToStep",
      "name": "Copy the Bank Details",
      "text": "You'll see our BSB, account number, and PayID details on the checkout confirmation page."
    },
    {
      "@type": "HowToStep",
      "name": "Transfer the Funds",
      "text": "Open your banking app and send the payment via PayID (instant) or BSB transfer. Use your order reference number."
    },
    {
      "@type": "HowToStep",
      "name": "Confirm Payment",
      "text": "Email or upload a screenshot of your bank transfer confirmation. We'll verify and start processing your order."
    }
  ]
};

const breadcrumbSchema = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://mullawaysmedicalcannabis.com.au/" },
    { "@type": "ListItem", "position": 2, "name": "Pay with Bank Transfer", "item": "https://mullawaysmedicalcannabis.com.au/pay-with-bank-transfer/" }
  ]
};

const transferMethods = [
  {
    name: 'PayID / Osko',
    speed: 'Instant (24/7)',
    fee: 'Free',
    availability: 'All major banks',
    icon: 'zap',
    recommended: true,
    description: 'The fastest way to pay. Transfers are instant, even on weekends and public holidays. Supported by all major Australian banks.',
  },
  {
    name: 'Standard Bank Transfer (BSB)',
    speed: '1-2 business hours',
    fee: 'Free',
    availability: 'All banks & credit unions',
    icon: 'bank',
    recommended: false,
    description: 'A classic bank-to-bank transfer using BSB and account number. Free with all banks, usually arrives within a few hours during business days.',
  },
  {
    name: 'BPAY',
    speed: '1-2 business days',
    fee: 'Free',
    availability: 'All banks with BPAY',
    icon: 'credit-card',
    recommended: false,
    description: 'Use our BPAY biller code and reference number. Takes 1-2 business days to clear, but widely available and easy to use.',
  },
];

const banks = [
  { name: 'Commonwealth Bank', supports: ['PayID', 'Osko', 'BSB', 'BPAY'], color: '#FFD100' },
  { name: 'Westpac', supports: ['PayID', 'Osko', 'BSB', 'BPAY'], color: '#D5002B' },
  { name: 'ANZ', supports: ['PayID', 'Osko', 'BSB', 'BPAY'], color: '#007DBA' },
  { name: 'NAB', supports: ['PayID', 'Osko', 'BSB', 'BPAY'], color: '#C8102E' },
  { name: 'ING', supports: ['PayID', 'Osko', 'BSB'], color: '#FF6200' },
  { name: 'Macquarie', supports: ['PayID', 'Osko', 'BSB'], color: '#000000' },
  { name: 'Bendigo Bank', supports: ['PayID', 'BSB', 'BPAY'], color: '#BE1E2D' },
  { name: 'Bank of Queensland', supports: ['PayID', 'BSB', 'BPAY'], color: '#00263A' },
];

const faqs = [
  {
    q: 'How do I pay with bank transfer?',
    a: 'At checkout, select "Bank Transfer" and place your order. You\'ll see our bank details (BSB & account number) and PayID. Open your banking app, send the payment using the exact total shown, and include your order reference number. Then email a screenshot of the transfer to confirm.'
  },
  {
    q: 'What is PayID and how does it work?',
    a: 'PayID is an instant payment system built into all major Australian banking apps. Instead of entering a BSB and account number, you just enter our PayID (email or phone number) and the money arrives within seconds — even on weekends. It\'s free, instant, and supported by all big four banks.'
  },
  {
    q: 'How long does a bank transfer take?',
    a: 'PayID/Osko transfers are instant (24/7). Standard BSB transfers usually arrive within 1-2 hours during business days. BPAY takes 1-2 business days. We recommend PayID for the fastest experience.'
  },
  {
    q: 'Are there any fees for bank transfer?',
    a: 'No. Bank transfers, PayID, and Osko payments are completely free for both you and us. There are no processing fees, surcharges, or hidden costs. This is why bank transfer is one of the best-value payment options.'
  },
  {
    q: 'Do I need to confirm my payment?',
    a: 'Yes. After transferring the funds, please email a screenshot or receipt of the transfer to our payments team using the Contact page with your order reference. This helps us match your payment and process your order faster.'
  },
  {
    q: 'What if I send the wrong amount?',
    a: 'If you underpay, we\'ll contact you to arrange the balance. If you overpay, we\'ll refund the difference to your bank account. Always double-check the exact total from your order confirmation before transferring.'
  },
  {
    q: 'Is bank transfer safe?',
    a: 'Yes. Australian bank transfers are protected by bank-level security and encryption. All transactions are traceable and protected by Australian banking regulations. Unlike cash, there\'s always a record of your payment.'
  },
  {
    q: 'Which banks support PayID?',
    a: 'All major Australian banks support PayID including Commonwealth Bank, Westpac, ANZ, NAB, ING, Macquarie, Bendigo Bank, Bank of Queensland, Suncorp, and most credit unions. Check your banking app — if you see a "PayID" option, you\'re good to go.'
  },
];
---

<Layout title={title} description={description} schema={[schema, breadcrumbSchema]}>
  <Header />
  <main>
  <Breadcrumb items={[{label: 'Home', href: '/'}, {label: 'Pay with Bank Transfer'}]} />

  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-bg">
      <div class="hero-gradient"></div>
      <div class="hero-pattern"></div>
    </div>
    <div class="container">
      <div class="hero-content">
        <div class="hero-badge">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><rect x="3" y="10" width="18" height="11" rx="1"/><path d="M3 10l9-7 9 7"/></svg>
          <span>Direct Bank Payment</span>
        </div>
        <h1>Pay with Bank Transfer <span class="text-gradient">in Australia</span></h1>
        <p class="hero-description">The simplest payment method. Transfer funds directly from your Australian bank account using PayID (instant) or BSB transfer. No sign-ups, no middlemen, zero fees.</p>
        <div class="hero-stats">
          <div class="hero-stat">
            <span class="stat-icon">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
            </span>
            <div>
              <span class="stat-value">Instant</span>
              <span class="stat-label">Via PayID/Osko</span>
            </div>
          </div>
          <div class="hero-stat">
            <span class="stat-icon">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 1v22M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>
            </span>
            <div>
              <span class="stat-value">$0</span>
              <span class="stat-label">Zero Fees</span>
            </div>
          </div>
          <div class="hero-stat">
            <span class="stat-icon">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
            </span>
            <div>
              <span class="stat-value">Secure</span>
              <span class="stat-label">Bank-Level Encryption</span>
            </div>
          </div>
        </div>
        <div class="hero-ctas">
          <a href="#how-it-works" class="btn btn-primary btn-large">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="7" width="20" height="14" rx="2"/><path d="M16 3l-4 4-4-4"/></svg>
            How It Works
          </a>
          <a href="#transfer-methods" class="btn btn-outline-light btn-large">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/></svg>
            Transfer Methods
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Why Bank Transfer -->
  <section class="section why-bank">
    <div class="container">
      <div class="section-header">
        <span class="section-label">Why Pay With Bank Transfer?</span>
        <h2>Benefits of Bank Transfer Payments</h2>
        <p>Bank transfer is the most straightforward way to pay. No apps to download, no accounts to create — just your regular banking app.</p>
      </div>
      <div class="benefits-grid">
        <div class="benefit-card">
          <div class="benefit-icon">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 1v22M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>
          </div>
          <h3>Zero Fees</h3>
          <p>Bank transfers are completely free — no processing fees, no surcharges, no hidden costs. You pay the exact price.</p>
        </div>
        <div class="benefit-card">
          <div class="benefit-icon">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
          </div>
          <h3>Instant with PayID</h3>
          <p>PayID/Osko transfers arrive within seconds — even on weekends and late at night. Your order starts processing immediately.</p>
        </div>
        <div class="benefit-card">
          <div class="benefit-icon">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
          </div>
          <h3>Nothing to Download</h3>
          <p>No extra apps, no extensions. Use the banking app you already have on your phone. If you can pay a bill, you can pay us.</p>
        </div>
        <div class="benefit-card">
          <div class="benefit-icon">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
          </div>
          <h3>Bank-Level Security</h3>
          <p>Protected by your bank's encryption and security measures. Every transfer is fully traceable and protected by Australian banking law.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- How It Works -->
  <section class="section how-section" id="how-it-works">
    <div class="container">
      <div class="section-header">
        <span class="section-label">Step-by-Step Guide</span>
        <h2>How to Pay with Bank Transfer <span class="text-accent">(4 Steps)</span></h2>
        <p>Follow these simple steps to complete your payment. The whole process takes just a few minutes.</p>
      </div>

      <div class="steps-container">
        <div class="step-card">
          <div class="step-number">1</div>
          <div class="step-content">
            <h3>Place Your Order</h3>
            <p>Browse our <a href="/shop/">shop</a>, add products to your cart, fill in your delivery information at <a href="/checkout/">checkout</a>, and select <strong>"Bank Transfer"</strong> as your payment method.</p>
          </div>
        </div>

        <div class="step-card">
          <div class="step-number">2</div>
          <div class="step-content">
            <h3>Copy Our Bank Details</h3>
            <p>After placing your order, you'll see our bank details. Copy them into your banking app:</p>
            <div class="bank-details-visual">
              <div class="bank-detail-row">
                <span class="bank-detail-label">Account Name</span>
                <span class="bank-detail-value">Mullaways Medical Pty Ltd</span>
              </div>
              <div class="bank-detail-row">
                <span class="bank-detail-label">BSB</span>
                <span class="bank-detail-value">062-000</span>
              </div>
              <div class="bank-detail-row">
                <span class="bank-detail-label">Account Number</span>
                <span class="bank-detail-value">1234 5678</span>
              </div>
              <div class="bank-detail-row">
                <span class="bank-detail-label">Reference</span>
                <span class="bank-detail-value highlight">Your Order Ref + Last Name</span>
              </div>
            </div>
            <div class="step-tip">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
              <span>Always include your order reference number so we can match your payment to your order</span>
            </div>
          </div>
        </div>

        <div class="step-card">
          <div class="step-number">3</div>
          <div class="step-content">
            <h3>Send the Payment</h3>
            <p>Open your banking app and transfer the <strong>exact amount</strong> shown on your order confirmation. Choose the method that suits you best:</p>
            <ul class="step-list">
              <li><strong>PayID / Osko</strong> — Instant, use our PayID identifier</li>
              <li><strong>BSB Transfer</strong> — Enter our BSB & account number, 1-2 hours</li>
              <li><strong>BPAY</strong> — Use our biller code and reference, 1-2 business days</li>
            </ul>
            <div class="step-tip">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
              <span>We recommend PayID for the fastest processing — payments arrive instantly 24/7</span>
            </div>
          </div>
        </div>

        <div class="step-card">
          <div class="step-number">4</div>
          <div class="step-content">
            <h3>Confirm Your Payment</h3>
            <p>After transferring, email a screenshot of the payment confirmation via our <a href="/contact/">contact page</a> with your order reference number.</p>
            <div class="step-tip success">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
              <span>Done! Once we verify your transfer, we'll start processing and shipping your order right away.</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Transfer Methods Comparison -->
  <section class="section methods-section" id="transfer-methods">
    <div class="container">
      <div class="section-header">
        <span class="section-label">Choose Your Method</span>
        <h2>Transfer Methods <span class="text-accent">Compared</span></h2>
        <p>Pick the transfer method that best suits your needs. We recommend PayID for the fastest experience.</p>
      </div>

      <div class="methods-grid">
        {transferMethods.map((method) => (
          <div class={`method-card ${method.recommended ? 'recommended' : ''}`}>
            {method.recommended && <div class="method-badge">
              <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              Recommended
            </div>}
            <div class="method-icon">
              {method.icon === 'zap' ? (
                <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
              ) : method.icon === 'bank' ? (
                <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="10" width="18" height="11" rx="1"/><path d="M3 10l9-7 9 7"/><line x1="7" y1="14" x2="7" y2="17"/><line x1="12" y1="14" x2="12" y2="17"/><line x1="17" y1="14" x2="17" y2="17"/></svg>
              ) : (
                <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="1" y="4" width="22" height="16" rx="2" ry="2"/><line x1="1" y1="10" x2="23" y2="10"/></svg>
              )}
            </div>
            <h3>{method.name}</h3>
            <p class="method-desc">{method.description}</p>
            <div class="method-stats">
              <div class="method-stat">
                <span class="ms-label">Speed</span>
                <span class="ms-value">{method.speed}</span>
              </div>
              <div class="method-stat">
                <span class="ms-label">Fee</span>
                <span class="ms-value accent">{method.fee}</span>
              </div>
              <div class="method-stat">
                <span class="ms-label">Available</span>
                <span class="ms-value">{method.availability}</span>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Supported Banks -->
  <section class="section banks-section">
    <div class="container">
      <div class="section-header">
        <span class="section-label">Compatibility</span>
        <h2>Supported Australian Banks</h2>
        <p>Bank transfer payments work with all Australian banks and credit unions. Here are the most popular ones.</p>
      </div>

      <div class="banks-grid">
        {banks.map((bank) => (
          <div class="bank-card">
            <div class="bank-icon" style={`background: ${bank.color}12; border-color: ${bank.color}30;`}>
              <span class="bank-initial" style={`color: ${bank.color};`}>{bank.name.charAt(0)}</span>
            </div>
            <div class="bank-info">
              <h4>{bank.name}</h4>
              <div class="bank-tags">
                {bank.supports.map((method: string) => (
                  <span class="bank-tag">{method}</span>
                ))}
              </div>
            </div>
          </div>
        ))}
      </div>

      <div class="banks-note">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
        <p>All Australian banks and credit unions support BSB transfers. PayID/Osko is supported by over 100 Australian financial institutions. If your bank isn't listed here, it almost certainly works too.</p>
      </div>
    </div>
  </section>

  <!-- Tips Section -->
  <section class="section tips-section">
    <div class="container">
      <div class="section-header">
        <span class="section-label">Helpful Tips</span>
        <h2>Tips for Bank Transfer Payments</h2>
      </div>
      <div class="tips-grid">
        <div class="tip-card">
          <div class="tip-number">01</div>
          <h3>Use PayID for Speed</h3>
          <p>PayID transfers are instant 24/7 — even on weekends and public holidays. It's by far the fastest way to pay and get your order processing.</p>
        </div>
        <div class="tip-card">
          <div class="tip-number">02</div>
          <h3>Include Your Reference</h3>
          <p>Always add your order reference number in the transfer description. This helps us match your payment & process your order faster.</p>
        </div>
        <div class="tip-card">
          <div class="tip-number">03</div>
          <h3>Send the Exact Amount</h3>
          <p>Transfer the exact total from your order confirmation. This avoids delays from under/overpayments needing manual adjustment.</p>
        </div>
        <div class="tip-card">
          <div class="tip-number">04</div>
          <h3>Screenshot the Transfer</h3>
          <p>Take a screenshot of the transfer confirmation and email it to us. This speeds up verification and order processing significantly.</p>
        </div>
        <div class="tip-card">
          <div class="tip-number">05</div>
          <h3>Check Processing Times</h3>
          <p>PayID: instant. BSB transfer: 1-2 hours (business hours). BPAY: 1-2 business days. Plan accordingly for time-sensitive orders.</p>
        </div>
        <div class="tip-card">
          <div class="tip-number">06</div>
          <h3>Keep Your Receipt</h3>
          <p>Your bank will provide a transaction receipt or reference number. Save this as proof of payment in case it's ever needed.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Other Payment Methods -->
  <section class="section other-payments">
    <div class="container">
      <div class="section-header">
        <span class="section-label">Other Options</span>
        <h2>Other Payment Methods</h2>
        <p>Bank transfer not right for you? We accept other payment methods to suit your preference.</p>
      </div>
      <div class="other-grid">
        <a href="/buy-bitcoin/" class="other-card">
          <div class="other-icon bitcoin">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="12" r="10"/><path d="M9.5 7.5V9H8V11H9.5V16.5H11.5V11H13.5L14 9H11.5V7.8C11.5 7.36 11.72 7.15 12.22 7.15H14V5H12.22C10.24 5 9.5 6.06 9.5 7.5Z"/></svg>
          </div>
          <div>
            <h3>Bitcoin</h3>
            <p>Buy Bitcoin online or at ATMs. Fast, private, and low fees.</p>
          </div>
          <svg class="arrow" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
        <a href="/pay-with-paysafecard/" class="other-card">
          <div class="other-icon paysafe">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="5" width="20" height="14" rx="2"/><path d="M2 10h20"/><path d="M6 15h4"/></svg>
          </div>
          <div>
            <h3>Paysafecard</h3>
            <p>Prepaid voucher — buy with cash, pay with a 16-digit PIN. Anonymous.</p>
          </div>
          <svg class="arrow" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
      </div>
    </div>
  </section>

  <!-- FAQ Section -->
  <section class="section faq-section" id="faq">
    <div class="container">
      <div class="section-header">
        <span class="section-label">Common Questions</span>
        <h2>Bank Transfer FAQ</h2>
        <p>Everything you need to know about paying with bank transfer.</p>
      </div>
      <div class="faq-container">
        {faqs.map((faq) => (
          <details class="faq-item">
            <summary class="faq-question">
              <span class="faq-q-text">{faq.q}</span>
              <span class="faq-toggle">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9l6 6 6-6"/></svg>
              </span>
            </summary>
            <div class="faq-answer">
              <p>{faq.a}</p>
            </div>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="section cta-section">
    <div class="container">
      <div class="cta-card">
        <div class="cta-content">
          <h2>Ready to Place Your Order?</h2>
          <p>Browse our lab-tested cannabis products and pay with a simple bank transfer — zero fees, zero hassle.</p>
          <div class="cta-buttons">
            <a href="/shop/" class="btn btn-primary btn-large">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/><line x1="3" y1="6" x2="21" y2="6"/><path d="M16 10a4 4 0 0 1-8 0"/></svg>
              Browse Products
            </a>
            <a href="/checkout/" class="btn btn-outline">
              Go to Checkout
            </a>
          </div>
        </div>
        <div class="cta-decoration">
          <svg width="120" height="120" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="0.5" opacity="0.2"/>
            <rect x="3" y="10" width="18" height="11" rx="1" stroke="currentColor" stroke-width="0.5" opacity="0.15"/>
            <path d="M3 10l9-7 9 7" stroke="currentColor" stroke-width="0.5" opacity="0.15"/>
          </svg>
        </div>
      </div>
    </div>
  </section>
  </main>

  <Footer />
</Layout>

<style>
  /* Hero */
  .hero {
    position: relative;
    padding: var(--space-3xl) 0 var(--space-4xl);
    overflow: hidden;
    min-height: 550px;
    display: flex;
    align-items: center;
  }

  .hero h1 {
    color: white;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .hero-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, #0a1628 0%, #0d2137 30%, #0a2810 70%, #0a1628 100%);
  }

  .hero-pattern {
    position: absolute;
    inset: 0;
    background-image:
      radial-gradient(circle at 20% 80%, rgba(45, 90, 74, 0.08) 0%, transparent 50%),
      radial-gradient(circle at 80% 20%, rgba(45, 90, 74, 0.06) 0%, transparent 50%);
    opacity: 0.8;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    max-width: 720px;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 16px;
    background: rgba(45, 90, 74, 0.15);
    border: 1px solid rgba(45, 90, 74, 0.3);
    border-radius: 100px;
    font-size: 0.85rem;
    font-weight: 600;
    color: #6dd5a0;
    margin-bottom: var(--space-lg);
  }

  h1 {
    font-size: clamp(2rem, 5vw, 3rem);
    margin-bottom: var(--space-lg);
    line-height: 1.15;
  }

  .text-gradient {
    background: linear-gradient(135deg, #2D5A4A 0%, #6dd5a0 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .text-accent {
    color: var(--color-primary);
  }

  .hero-description {
    color: rgba(255, 255, 255, 0.75);
    font-size: 1.15rem;
    line-height: 1.7;
    max-width: 620px;
    margin-bottom: var(--space-xl);
  }

  .hero-stats {
    display: flex;
    gap: var(--space-xl);
    margin-bottom: var(--space-xl);
    flex-wrap: wrap;
  }

  .hero-stat {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
  }

  .stat-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: var(--radius-md);
    background: rgba(45, 90, 74, 0.12);
    color: #6dd5a0;
  }

  .stat-value {
    display: block;
    font-weight: 700;
    color: white;
    font-size: 1.1rem;
  }

  .stat-label {
    display: block;
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.5);
  }

  .hero-ctas {
    display: flex;
    gap: var(--space-md);
    flex-wrap: wrap;
  }

  /* Why Bank Transfer */
  .why-bank {
    background: white;
  }

  .section-header {
    text-align: center;
    max-width: 640px;
    margin: 0 auto var(--space-3xl);
  }

  .section-header p {
    font-size: 1.05rem;
    color: var(--color-text-light);
  }

  .benefits-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-xl);
  }

  .benefit-card {
    text-align: center;
    padding: var(--space-xl);
    border-radius: var(--radius-lg);
    border: 1px solid var(--color-border);
    transition: all var(--transition-base);
  }

  .benefit-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .benefit-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 56px;
    height: 56px;
    border-radius: var(--radius-md);
    background: rgba(45, 90, 74, 0.08);
    color: var(--color-primary);
    margin-bottom: var(--space-md);
  }

  .benefit-card h3 {
    font-size: 1.1rem;
    margin-bottom: var(--space-sm);
  }

  .benefit-card p {
    font-size: 0.9rem;
    color: var(--color-text-light);
    margin: 0;
    line-height: 1.6;
  }

  /* Steps */
  .how-section {
    background: var(--color-bg);
  }

  .steps-container {
    max-width: 780px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: var(--space-xl);
  }

  .step-card {
    display: flex;
    gap: var(--space-xl);
    background: white;
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
    border: 1px solid var(--color-border);
    transition: all var(--transition-base);
  }

  .step-card:hover {
    box-shadow: var(--shadow-md);
  }

  .step-number {
    flex-shrink: 0;
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 1.2rem;
  }

  .step-content h3 {
    font-size: 1.15rem;
    margin-bottom: var(--space-sm);
  }

  .step-content p {
    color: var(--color-text-light);
    font-size: 0.95rem;
    line-height: 1.6;
    margin-bottom: var(--space-md);
  }

  .step-list {
    margin: var(--space-md) 0;
    padding-left: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .step-list li {
    font-size: 0.92rem;
    color: var(--color-text-light);
    padding: 8px 12px;
    background: var(--color-bg);
    border-radius: var(--radius-sm);
    list-style: none;
  }

  .step-tip {
    display: flex;
    align-items: flex-start;
    gap: var(--space-sm);
    padding: var(--space-md);
    background: rgba(45, 90, 74, 0.06);
    border-radius: var(--radius-md);
    font-size: 0.85rem;
    color: var(--color-primary);
    border-left: 3px solid var(--color-primary);
  }

  .step-tip svg {
    flex-shrink: 0;
    margin-top: 2px;
  }

  .step-tip.success {
    background: rgba(16, 185, 129, 0.08);
    border-left-color: #10b981;
    color: #065f46;
  }

  /* Bank Details Visual */
  .bank-details-visual {
    margin: var(--space-md) 0;
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    overflow: hidden;
  }

  .bank-detail-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 12px 16px;
    border-bottom: 1px solid var(--color-border);
  }

  .bank-detail-row:last-child {
    border-bottom: none;
  }

  .bank-detail-label {
    font-size: 0.85rem;
    color: var(--color-text-muted);
  }

  .bank-detail-value {
    font-weight: 600;
    font-size: 0.95rem;
    color: var(--color-text);
  }

  .bank-detail-value.highlight {
    color: var(--color-primary);
  }

  /* Transfer Methods */
  .methods-section {
    background: white;
  }

  .methods-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
  }

  .method-card {
    padding: var(--space-xl);
    border-radius: var(--radius-lg);
    border: 1px solid var(--color-border);
    text-align: center;
    position: relative;
    transition: all var(--transition-base);
  }

  .method-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .method-card.recommended {
    border-color: var(--color-primary);
    background: linear-gradient(180deg, rgba(45, 90, 74, 0.03) 0%, white 40%);
    box-shadow: 0 4px 24px rgba(45, 90, 74, 0.08);
  }

  .method-badge {
    position: absolute;
    top: 0;
    right: 0;
    display: inline-flex;
    align-items: center;
    gap: 4px;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
    color: white;
    font-size: 0.72rem;
    font-weight: 600;
    padding: 6px 14px 6px 12px;
    border-radius: 0 var(--radius-lg) 0 var(--radius-lg);
    white-space: nowrap;
  }

  .method-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 56px;
    height: 56px;
    border-radius: var(--radius-md);
    background: rgba(45, 90, 74, 0.08);
    color: var(--color-primary);
    margin-bottom: var(--space-md);
  }

  .method-card h3 {
    font-size: 1.1rem;
    margin-bottom: var(--space-sm);
  }

  .method-desc {
    font-size: 0.9rem;
    color: var(--color-text-light);
    line-height: 1.6;
    margin-bottom: var(--space-lg);
  }

  .method-stats {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .method-stat {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 8px 12px;
    background: var(--color-bg);
    border-radius: var(--radius-sm);
  }

  .ms-label {
    font-size: 0.82rem;
    color: var(--color-text-muted);
  }

  .ms-value {
    font-weight: 600;
    font-size: 0.88rem;
    color: var(--color-text);
  }

  .ms-value.accent {
    color: var(--color-primary);
  }

  /* Banks */
  .banks-section {
    background: var(--color-bg);
  }

  .banks-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-md);
    margin-bottom: var(--space-xl);
  }

  .bank-card {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    padding: var(--space-lg);
    background: white;
    border-radius: var(--radius-md);
    border: 1px solid var(--color-border);
    transition: all var(--transition-base);
  }

  .bank-card:hover {
    box-shadow: var(--shadow-sm);
    transform: translateY(-2px);
  }

  .bank-icon {
    flex-shrink: 0;
    width: 40px;
    height: 40px;
    border-radius: 10px;
    border: 1.5px solid;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .bank-initial {
    font-size: 1.1rem;
    font-weight: 700;
    font-family: var(--font-display);
  }

  .bank-info h4 {
    font-size: 0.88rem;
    margin-bottom: 4px;
  }

  .bank-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 4px;
  }

  .bank-tag {
    padding: 2px 7px;
    background: var(--color-bg);
    border-radius: 4px;
    font-size: 0.68rem;
    color: var(--color-text-muted);
    font-weight: 500;
  }

  .banks-note {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    max-width: 700px;
    margin: 0 auto;
    padding: var(--space-md) var(--space-lg);
    background: rgba(45, 90, 74, 0.06);
    border-radius: var(--radius-md);
    font-size: 0.88rem;
    color: var(--color-text-light);
  }

  .banks-note svg {
    flex-shrink: 0;
    color: var(--color-primary);
    margin-top: 2px;
  }

  .banks-note p {
    margin: 0;
  }

  /* Tips */
  .tips-section {
    background: white;
  }

  .tips-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
  }

  .tip-card {
    padding: var(--space-xl);
    border-radius: var(--radius-lg);
    border: 1px solid var(--color-border);
    transition: all var(--transition-base);
  }

  .tip-card:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .tip-number {
    font-size: 2rem;
    font-weight: 800;
    color: rgba(45, 90, 74, 0.12);
    line-height: 1;
    margin-bottom: var(--space-sm);
  }

  .tip-card h3 {
    font-size: 1.05rem;
    margin-bottom: var(--space-sm);
  }

  .tip-card p {
    font-size: 0.9rem;
    color: var(--color-text-light);
    line-height: 1.6;
    margin: 0;
  }

  /* Other Payments */
  .other-payments {
    background: var(--color-bg);
  }

  .other-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-lg);
    max-width: 700px;
    margin: 0 auto;
  }

  .other-card {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    padding: var(--space-xl);
    background: white;
    border-radius: var(--radius-lg);
    border: 1px solid var(--color-border);
    text-decoration: none;
    color: inherit;
    transition: all var(--transition-base);
  }

  .other-card:hover {
    transform: translateY(-3px);
    box-shadow: var(--shadow-md);
    border-color: var(--color-primary);
  }

  .other-card:hover .arrow {
    transform: translateX(3px);
    color: var(--color-primary);
  }

  .other-icon {
    flex-shrink: 0;
    width: 48px;
    height: 48px;
    border-radius: var(--radius-md);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .other-icon.bitcoin {
    background: rgba(247, 147, 26, 0.1);
    color: #f7931a;
  }

  .other-icon.paysafe {
    background: rgba(0, 163, 224, 0.1);
    color: #00a3e0;
  }

  .other-card h3 {
    font-size: 1rem;
    margin-bottom: 2px;
  }

  .other-card p {
    font-size: 0.85rem;
    color: var(--color-text-light);
    margin: 0;
  }

  .other-card .arrow {
    flex-shrink: 0;
    margin-left: auto;
    color: var(--color-text-muted);
    transition: all var(--transition-base);
  }

  /* FAQ */
  .faq-section {
    background: var(--color-bg);
  }

  .faq-container {
    max-width: 780px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .faq-item {
    background: white;
    border-radius: var(--radius-md);
    border: 1px solid var(--color-border);
    overflow: hidden;
    transition: all var(--transition-base);
  }

  .faq-item[open] {
    box-shadow: var(--shadow-md);
    border-color: var(--color-primary);
  }

  .faq-question {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: var(--space-lg) var(--space-xl);
    cursor: pointer;
    list-style: none;
    font-weight: 600;
    font-size: 1rem;
    gap: var(--space-md);
    transition: color var(--transition-base);
  }

  .faq-question::-webkit-details-marker {
    display: none;
  }

  .faq-question:hover {
    color: var(--color-primary);
  }

  .faq-toggle {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    border-radius: 50%;
    background: var(--color-bg);
    transition: all var(--transition-base);
  }

  .faq-item[open] .faq-toggle {
    transform: rotate(180deg);
    background: var(--color-primary);
    color: white;
  }

  .faq-answer {
    padding: 0 var(--space-xl) var(--space-lg);
  }

  .faq-answer p {
    color: var(--color-text-light);
    line-height: 1.7;
    font-size: 0.95rem;
    margin: 0;
  }

  /* CTA */
  .cta-section {
    background: white;
  }

  .cta-card {
    background: linear-gradient(135deg, var(--color-eucalyptus-dark) 0%, var(--color-eucalyptus) 100%);
    border-radius: var(--radius-xl);
    padding: var(--space-3xl) var(--space-4xl);
    position: relative;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .cta-content {
    position: relative;
    z-index: 1;
    max-width: 540px;
  }

  .cta-content h2 {
    color: white;
    margin-bottom: var(--space-md);
  }

  .cta-content p {
    color: rgba(255, 255, 255, 0.8);
    margin-bottom: var(--space-xl);
    line-height: 1.7;
  }

  .cta-buttons {
    display: flex;
    gap: var(--space-md);
    flex-wrap: wrap;
  }

  .cta-decoration {
    color: rgba(255, 255, 255, 0.1);
    position: relative;
    z-index: 1;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .benefits-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .methods-grid {
      grid-template-columns: 1fr;
      max-width: 500px;
      margin: 0 auto;
    }

    .banks-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .hero {
      min-height: auto;
      padding: var(--space-2xl) 0 var(--space-3xl);
    }

    .hero-stats {
      gap: var(--space-lg);
    }

    .step-card {
      flex-direction: column;
      gap: var(--space-md);
    }

    .tips-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .cta-card {
      padding: var(--space-2xl);
    }

    .cta-decoration {
      display: none;
    }

    .other-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 480px) {
    .benefits-grid,
    .banks-grid {
      grid-template-columns: 1fr;
    }

    .tips-grid {
      grid-template-columns: 1fr;
    }

    .hero-ctas {
      flex-direction: column;
    }

    .bank-details-visual {
      font-size: 0.9rem;
    }
  }
</style>

<script>
  // FAQ toggle animation
  document.querySelectorAll('.faq-item').forEach(item => {
    item.addEventListener('toggle', () => {
      if ((item as HTMLDetailsElement).open) {
        const answer = item.querySelector('.faq-answer') as HTMLElement;
        if (answer) {
          answer.style.opacity = '0';
          answer.style.transform = 'translateY(-8px)';
          requestAnimationFrame(() => {
            answer.style.transition = 'opacity 0.3s ease, transform 0.3s ease';
            answer.style.opacity = '1';
            answer.style.transform = 'translateY(0)';
          });
        }
      }
    });
  });
</script>
